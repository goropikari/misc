FROM golang:1.24-alpine AS build
WORKDIR /src
COPY go.mod go.sum ./
RUN go mod download
COPY main.go .
RUN CGO_ENABLED=0 go build -trimpath -ldflags='-s -w' -o /out/log-server .

FROM alpine:3.22
RUN adduser -D -u 10001 app
USER app
COPY --from=build /out/log-server /log-server
EXPOSE 8080
ENTRYPOINT ["/log-server"]
